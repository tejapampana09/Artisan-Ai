[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["backend/tests"]
pythonpath = ["."]

[tool.pyright]
include = ["backend"]
exclude = ["**/node_modules", "**/__pycache__", ".venv", "venv"]
venvPath = "."
extraPaths = [
    ".",
    "backend"
]
